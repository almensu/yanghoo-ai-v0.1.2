{
  "name": "src",
  "version": "1.0.0",
  "description": "This module provides a simple task scheduler for the Content Studio pipeline. It reads manifest files, finds tasks with state=\"queued\", executes the appropriate worker, and updates the manifest and library.json.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/fs-extra": "^11.0.4",
    "fs-extra": "^11.3.0"
  }
}
